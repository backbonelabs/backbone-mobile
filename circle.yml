machine:
  timezone:
    America/Los_Angeles
  node:
    version: 6.9.1
dependencies:
  cache_directories:
    - ~/.cache/yarn
  override:
    - yarn
test:
  override:
    - npm run lint
    - npm test
